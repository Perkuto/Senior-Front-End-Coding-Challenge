go to project directory

type: npm install (you will have to make sure your nodejs is around or greater than version 8.11.2)

type: npm run build (to create production build)

type: serve -s build (to start production server may have to first run: npm install -g serve)

type: npm start (to start dev build)

